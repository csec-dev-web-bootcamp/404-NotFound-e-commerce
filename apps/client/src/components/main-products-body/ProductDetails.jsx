import React from "react";

const ProductDetails = ({ product }) => {
  return (
    <div>
      This is detail of product -- {product.title}
      ProductDetails
    </div>
  );
};

export default ProductDetails;
